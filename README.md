<!-- git init
git add .
git add <file name>
git commit -m <message>
git status
git push -u origin <branch name> -->

git branch main = to make a main branch in github
git branch day3 = to add branches to the main brach in github
git branch d4 = same as above
git branch -D d4 = to delete a branch
git checkout day3 = to swap to another branch

remember to always make branches in the main hub for day topics n such as
make smaller branches in the correlated branch, so that there is no overlapping n its not confusing
always make sure that the branch u r adding is in the correct branch
